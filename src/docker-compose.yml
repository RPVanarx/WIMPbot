version: "3.1"
services:
  db:
    image: postgres:11-alpine
    restart: always
    env_file:
      - ./.env.db
    volumes:
      - ./.base/data:/var/lib/postgresql/data
    ports:
      - 5431:5432
  web:
    image: wimp
    restart: always
    depends_on:
      - db
    ports:
      - 3030:3000
  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080
